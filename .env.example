# Supabase (public — safe for static GitHub Pages build)
# Add these in GitHub: Settings → Secrets and variables → Actions → Variables
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Set in Supabase Dashboard → Edge Functions → waitlist-signup → Secrets
# ALLOWED_ORIGINS=https://myquest.live,http://localhost:3000
